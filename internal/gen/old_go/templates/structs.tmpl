package gen

{{ range $struct := .Structs }}
{{ range $docLine := $struct.Comments }}
// {{ $docLine }}{{ end }}
type {{ $struct.Name }} struct {

    {{ range $field := $struct.Fields }}
    {{ $field.Name }} {{ if $field.Optional }}*{{ end }}{{ $field.Type }}{{ with $field.Tags }} `{{ join . " " }}`{{ end }}
    {{ end }}
}
{{ end }}
